# Example environment variables for production
# Copy to /etc/mstatili/env (no quotes) or another secure location and update `mstatili-backend.service` EnvironmentFile path

MONGO_URL=mongodb://mongo:27017/mstatili_db
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
NOTIFICATION_EMAIL=
# Optional app settings
# PORT (backend port - default uses 8000 in the systemd/gunicorn service)
